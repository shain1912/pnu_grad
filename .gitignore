node_modules/
dist/
.env
.env.local
.env.*.local
*.sqlite
*.sqlite-shm
*.sqlite-wal
backend/credentials/
*.log
.DS_Store

# 신청자 응답 CSV 내보내기 = 개인정보. 절대 커밋/배포 금지.
responses-*.csv
**/responses-*.csv

# Caddy 런타임
caddy/caddy.exe
caddy/access.log
caddy/access.log.*
# Caddy data/config — Windows에선 보통 %AppData%\Caddy 사용하지만 혹시 모를 누락 방지
caddy/data/
caddy/config/

# 로컬 미리보기/도구 작업물 (커밋 제외)
.devmock/
.shot/
# shadcn CLI 루트 툴링 (앱 아님)
/package.json
/package-lock.json
